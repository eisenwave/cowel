#ifndef COWEL_ASCII_ALGORITHM_HPP
#define COWEL_ASCII_ALGORITHM_HPP

#include "ulight/impl/ascii_algorithm.hpp"

namespace cowel::ascii {

using ulight::ascii::contains_ignore_case;
using ulight::ascii::equals_ignore_case;
using ulight::ascii::find_if;
using ulight::ascii::find_if_not;
using ulight::ascii::length_before;
using ulight::ascii::length_before_not;
using ulight::ascii::length_if;
using ulight::ascii::length_if_head_tail;
using ulight::ascii::length_if_not;
using ulight::ascii::length_until;
using ulight::ascii::length_until_not;
using ulight::ascii::starts_with_ignore_case;

} // namespace cowel::ascii

#endif
